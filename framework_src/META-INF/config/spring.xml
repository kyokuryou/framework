<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context" xmlns:tx="http://www.springframework.org/schema/tx"

       xsi:schemaLocation="http://www.springframework.org/schema/beans
                     http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
                     http://www.springframework.org/schema/context
                     http://www.springframework.org/schema/context/spring-context-3.1.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- 开启注解处理器 -->
    <context:annotation-config />
    <!-- 设置需要进行Spring注解扫描的类包 -->
    <context:component-scan base-package="com.xxx.xxx" use-default-filters="false">
        <!--  扫描符合@Service @Repository @Controller的类 -->
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Service" />
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Repository" />
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller" />
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Component" />
    </context:component-scan>

    <!-- 主要配置(必须) -->
    <import resource="classpath:spring-system.xml"/>
    <!-- webService支持,依赖spring-system.xml(可选)-->
    <import resource="classpath:ws_system.xml"/>
    <!-- 项目中webService配置,依赖spring-system.xml,ws_system.xml(可选)-->
    <import resource="classpath:spring_ws.xml"/>

    <!--jndi配置-->
    <bean id="xxxxDataSource" parent="jndi">
        <property name="jndiName" value="${xxxx.dataSource}"/>
    </bean>
    <!--jdbc配置-->
    <bean id="xxxxDataSource" parent="dataSource">
        <property name="driverClass" value="${xxxx.driver}"/>
        <property name="jdbcUrl" value="${xxxx.url}"/>
        <property name="user" value="${xxxx.username}"/>
        <property name="password" value="${xxxx.password}"/>
    </bean>
    <!--jndi配置与jdbc配置二者选一 -->
    <bean id="xxxxSqlSession" parent="sqlSession">
        <property name="dataSource" ref="xxxxDataSource"/>
        <!--如果不使用默认数据库类型,添加以下节点-->
        <property name="sqlType" value="Oracle"/>
    </bean>
    <!--对该dataSource增加注解事务管理 start-->
    <bean name="xxxxTM" parent="transactionManager">
        <property name="dataSource" ref="xxxxDataSource"/>
    </bean>
    <tx:annotation-driven proxy-target-class="true" transaction-manager="xxxxTM"/>
    <!--对该dataSource增加注解事务管理 end-->

    <!-- MyWebBoot继承AppBoot或WebBoot-->
    <bean class="com.xxx.xxx.MyWebBoot" init-method="main" destroy-method="exit" />


</beans>