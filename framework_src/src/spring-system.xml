<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"

       xsi:schemaLocation="http://www.springframework.org/schema/beans
                     http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
                     http://www.springframework.org/schema/aop
                     http://www.springframework.org/schema/aop/spring-aop-3.1.xsd
                     http://www.springframework.org/schema/context
                     http://www.springframework.org/schema/context/spring-context-3.1.xsd
                     http://www.springframework.org/schema/task
                     http://www.springframework.org/schema/task/spring-task.xsd">

    <!-- 开启注解处理器 -->
    <context:annotation-config/>
    <!-- 设置需要进行Spring注解扫描的类包 -->
    <context:component-scan base-package="org.smarty.core" use-default-filters="false">
        <!-- 扫描符合@Service的类 -->
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Component"/>
    </context:component-scan>
    <!-- 使用AspectJ方式配置AOP -->
    <aop:aspectj-autoproxy proxy-target-class="true"/>
    <aop:config proxy-target-class="true"/>
    <!-- 定时器任务 -->
    <task:annotation-driven/>
    <!-- 自定义定时任务-->
    <bean id="schedulerFactory" class="org.springframework.scheduling.quartz.SchedulerFactoryBean"/>
    <bean id="scheduleManager" class="org.smarty.core.support.ScheduleManager">
        <property name="scheduler" ref="schedulerFactory"/>
    </bean>

    <!-- JDBC参数配置 -->
    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer" lazy-init="true">
        <property name="order" value="0"/>
        <property name="systemPropertiesModeName" value="SYSTEM_PROPERTIES_MODE_OVERRIDE"/>
        <property name="ignoreResourceNotFound" value="true"/>
        <property name="locations" ref="configList"/>
    </bean>

    <!-- 数据源 -->
    <bean id="dataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource" destroy-method="close" abstract="true">
        <property name="maxPoolSize" value="${pool.c3p0.maxPoolSize}"/>
        <property name="minPoolSize" value="${pool.c3p0.minPoolSize}"/>
        <property name="initialPoolSize" value="${pool.c3p0.initialPoolSize}"/>
        <property name="acquireIncrement" value="${pool.c3p0.acquireIncrement}"/>
        <property name="maxStatements" value="${pool.c3p0.maxStatements}"/>
        <property name="maxIdleTime" value="${pool.c3p0.maxIdleTime}"/>
        <property name="idleConnectionTestPeriod" value="${pool.c3p0.idleConnectionTestPeriod}"/>
    </bean>
    <!-- JNDI -->
    <bean id="jndi" class="org.springframework.jndi.JndiObjectFactoryBean" abstract="true"/>
    <!-- 事务管理器 -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager"
          abstract="true" lazy-init="true"/>
    <!-- SQLSession -->
    <bean id="sqlSession" class="org.smarty.core.support.jdbc.SQLSession" abstract="true">
        <property name="sqlType" value="${default.db}"/>
    </bean>

    <!-- SMTP邮件服务配置 -->
    <bean id="javaMailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl" lazy-init="true">
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">true</prop>
                <prop key="mail.smtp.timeout">25000</prop>
                <!-- SSL连接配置 -->
                <!--
                    <prop key="mail.smtp.starttls.enable">true</prop>
                    <prop key="mail.smtp.socketFactory.class">javax.net.ssl.SSLSocketFactory</prop>
                    -->
            </props>
        </property>
    </bean>
    <!-- 邮件管理器 -->
    <bean id="mailManager" class="org.smarty.core.support.MailManager" lazy-init="true">
        <property name="javaMailSender" ref="javaMailSender"/>
        <property name="taskExecutor" ref="taskExecutor"/>
    </bean>
    <!-- SMTP邮件异步发送 -->
    <bean id="taskExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor" lazy-init="true">
        <!-- 核心线程数 -->
        <property name="corePoolSize" value="10"/>
        <!-- 最大线程数 -->
        <property name="maxPoolSize" value="50"/>
        <!-- 最大队列数 -->
        <property name="queueCapacity" value="10000"/>
        <!-- 线程池维护线程所允许的空闲时间 -->
        <property name="keepAliveSeconds" value="60"/>
    </bean>
    <!-- 验证码 -->
    <bean id="captchaService" class="com.octo.captcha.service.image.DefaultManageableImageCaptchaService"
          lazy-init="true">
        <property name="captchaEngine">
            <bean class="org.smarty.core.support.captcha.CaptchaEngine"/>
        </property>
        <!--验证码过期时间-->
        <property name="minGuarantedStorageDelayInSeconds" value="600"/>
    </bean>

</beans>